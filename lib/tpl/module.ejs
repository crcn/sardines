define("<%-__filename %>", function(require, module, exports, __dirname, __filename) {
	 
  <%
    dependencies.forEach(function(dep) {
      content = content.replace(new RegExp("[\"\']"+dep.relPath+"[\"\']", "g"), "\""+dep.alias+"\"")
    });
  %>

  <%-content %>

  return module.exports;
});